import React from 'react'
import "./Background.scss";

const Background = ({children}) => {
    return (
        <div id="background">
            
            {children}
        </div>
    )
}

export default Background
